{
    "name": "nichetube-mvp",
    "version": "1.0.0",
    "scripts": {
        "build": "cd frontend && npm install && npx expo export -p web",
        "postinstall": "cd frontend && npm install"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "firebase-admin": "^11.8.0",
        "firebase-functions": "^4.3.1"
    },
    "engines": {
        "node": "18.x"
    }
}